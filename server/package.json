{
  "name": "repair-work-order-license-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "generate-keys": "node src/generate-keys.js",
    "test": "node --test"
  },
  "dependencies": {
    "mariadb": "^3.4.0"
  }
}
